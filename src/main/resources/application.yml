server:
  port: 8080
  address: 0.0.0.0

spring:
  profiles:
    active: dev

  application:
    name: order-service

management:
  endpoints:
    web:
      exposure:
        include: health,info

user:
  service:
    url: ${USER_SERVICE_URL}

product:
  service:
    url: ${PRODUCT_SERVICE_URL}
